import 'package:flutter/material.dart';

class Ovningar extends StatelessWidget {
  const Ovningar ({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Search Page"

    ),
    );
  }
 }